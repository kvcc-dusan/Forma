[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback (also covered by public/_redirects). The service worker handles
# offline navigation once installed.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
